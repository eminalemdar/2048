apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: game-2048
  labels:
    app: 2048-frontend
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: 2048-frontend
    component: frontend