apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: game-2048
  labels:
    app: 2048-backend
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: 2048-backend
    component: backend